#ifndef UTILS_H
#define UTILS_H

#include <stdint.h>
#include <limits.h>
#include <assert.h>

class Utils
{
public:
	static uint8_t rot_l(uint8_t);
	static uint8_t rot_r(uint8_t);
protected:
private:
};

#endif